//! Key-binding constants and helpers shared across modes.
//!
//! Scaffold placeholder. The mode switcher (Tab / Shift-Tab / q) is wired
//! directly in [`crate::tui::modes::handle_key`] at scaffold time; this
//! module is reserved for shared bindings such as `?` (help overlay),
//! `/` (focus search), and `f` (filter dialog) as those land.
